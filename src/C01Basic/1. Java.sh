자바언어와 다른 언어 성능 차이나는 이유

c언어는 운영서버에 이미 기계어로(이미 컴파일됨)
 - 단점은 코드를 운영서버에서 못 고침
 - 실행속도는 훨씬 빠름
파이썬은 운영서버에서 기계어로
 - 장점 코드를 운영서버에서 유연하게 고침

 컴파일한 파일 .class (바이트 코드)
 
우리가 보는 문법 .java

# 환경변수 잡기 <시스템 전역변수 설정>
- pc 속성 -> 고급시스템설정 -> 환경 변수 -> Path (자바 bin 경로) 넣기

# 자바 버전 1~24

java6 : 국내에서 가장 오랜된 버전

java8 : 최초의 현대화 단계,

java11 : 

java17 : + spring
 
java24 : + spring

# JDK == java